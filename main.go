package main

import (
	"time"
)

func main() {
	// gooo()
	// foogo()
	// fooor()
	// Unix()
	// fmt.Println(strings.IndexFunc("m*+&^]&./", s))
	// SSHMaker()
	<-time.After(time.Hour)
}

func s(c rune) bool {
	// if c != "]" {
	if c != ']' {
		return false
	}
	return true
}
